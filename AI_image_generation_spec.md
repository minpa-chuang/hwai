# AI 圖片生成系統優化規格書 (強調視覺化設計)

## 1. 核心設計理念

本系統旨在打造一個**沉浸式、視覺導向的 AI 創意工作台**。使用者不應感覺在操作一個呆板的表單，而應感覺在一個激發靈感的數位畫布上創作。所有功能設計都以「降低認知負擔」和「提升創作流暢度」為最高原則。

## 2. 介面佈局與視覺設計 (UI/UX)

建議採用一個**三欄式佈局**，並以現代化的深色主題 (Dark Mode) 作為預設風格，以減少視覺疲勞，讓生成的圖片內容更突出。

```
+--------------------------------+--------------------------------------+----------------------+
| [ 控制面板 (左欄) ]             | [ 主要工作區 (中欄) ]                  | [ 歷史畫廊 (右欄) ]   |
|                                |                                      |                      |
|  - 模型選擇器 (OpenAI/Gemini)    |  +--------------------------------+  |  - 圖片縮圖網格      |
|  - 提示詞輸入框 (Prompt)         |  |                                |  |  - 依日期分組        |
|  - 負面提示詞輸入框 (Negative)   |  |   這裡是圖片生成結果的展示區     |  |  - 搜尋功能          |
|  - 風格預設按鈕 (如: 寫實、動漫) |  |   或是一個引導使用者開始創作的   |  |                      |
|  - 畫面比例選擇 (1:1, 16:9)    |  |   精美佔位圖/動畫                |  |                      |
|  - ✨ 生成按鈕 ✨            |  |                                |  |                      |
|                                |  +--------------------------------+  |                      |
|  - 設定 (齒輪圖示)               |  - 放大、下載、複製提示詞等工具列   |  |                      |
|                                |                                      |                      |
+--------------------------------+--------------------------------------+----------------------+
```

### 2.1. 控制面板 (左欄 - The Cockpit)

* **模型選擇器:** 使用帶有 Logo 的下拉選單或分頁按鈕，讓使用者可以直觀地在 `OpenAI DALL-E` 和 `Google Gemini` 之間切換。
* **智慧提示詞輸入框:**
    * 支援多行輸入，並有語法高亮功能（例如將權重語法 `(word:1.2)` 用不同顏色標示）。
    * 提供「從歷史紀錄帶入」或「隨機靈感」的輔助按鈕。
* **風格預設 (Style Presets):** 一系列視覺化的卡片式按鈕，例如「寫實攝影」、「賽博龐克」、「水彩畫」、「二次元動漫」。點擊後會自動在提示詞中加入對應的風格關鍵字。
* **畫面比例 (Aspect Ratio):** 使用圖示化按鈕（例如正方形、垂直矩形、水平矩形）代替純文字選項，讓選擇更直觀。
* **生成按鈕:** 這是整個介面中最核心的按鈕。它應該有醒目的設計、流暢的微動畫，並在生成過程中轉變為進度條或取消按鈕。

### 2.2. 主要工作區 (中欄 - The Canvas)

* **初始狀態:** 在尚未生成任何圖片時，此區域不是空白的，而是顯示一個引導性的歡迎畫面，例如「請在左側輸入您的奇思妙想，開始第一張 AI 創作！」 的動態文字或是一個激發靈感的背景動畫。
* **生成中狀態:** 點擊生成後，此區域會顯示一個優雅的載入動畫（例如擴散的波紋、變換的幾何圖形），並顯示預估剩餘時間或進度。
* **結果展示:**
    * 圖片生成後會以高解析度在此區域展示。
    * 圖片下方會浮現一條**工具列**，提供「下載」、「放大檢視」、「複製提示詞」、「以此設定重新生成」等一鍵式操作。

### 2.3. 歷史畫廊 (右欄 - The Gallery)

* **持久化儲存:** 所有生成過的圖片及其設定（提示詞、模型、比例等）都會自動儲存在瀏覽器的 `IndexedDB` 中，實現永久的本地畫廊。
* **視覺化瀏覽:** 以瀑布流或網格佈局展示歷史圖片的縮圖。
* **互動功能:**
    * **懸停預覽 (Hover):** 滑鼠懸停在縮圖上時，會放大顯示並浮現提示詞。
    * **點擊載入 (Click):** 點擊任一張歷史圖片，會立即將該圖片載入到「主要工作區」，並同時將當時所有的生成設定（提示詞、模型、風格等）自動填入「控制面板」，方便使用者微調和二次創作。
    * **搜尋與篩選:** 提供關鍵字搜尋功能，可快速在歷史紀錄中找到相關創作。

## 3. 優化的互動流程 (User Flow)

1. **首次啟動:** 系統自動檢測 `localStorage` 中無 API 金鑰，彈出一個友善的設定引導畫面，而非僅僅無法操作。
2. **創作流程:**
    * 使用者在左側面板填寫或選擇參數。
    * 點擊中央下方閃爍的「生成」按鈕。
    * 主要工作區進入「生成中」狀態，右側歷史畫廊頂部出現一個「正在生成...」的佔位符。
    * 生成完成後，圖片在主要工作區優雅地淡入顯示，同時歷史畫廊的佔位符被真實的縮圖取代。
3. **二次創作:**
    * 使用者在右側畫廊看到一張滿意的舊作。
    * 點擊該縮圖。
    * 左側控制面板的**所有設定瞬間恢復**到生成該圖時的狀態。
    * 使用者只需修改提示詞中的一兩個詞，即可再次生成，極大地提升了迭代創作的效率。

## 4. 技術實現建議

* **前端框架:** 為了實現上述複雜的 UI 互動與狀態管理，強烈建議使用 **React** 或 **Vue**。
* **本地儲存:**
    * `localStorage`: 繼續用於儲存加密後的 API 金鑰。
    * `IndexedDB`: 用於儲存歷史生成紀錄（圖片 Blob 和相關設定），因其對大量結構化數據和二進制數據的儲存性能遠超 `localStorage`。
* **UI 元件庫:** 考慮使用如 **Ant Design** 或 **MUI** 等成熟的元件庫，並在其基礎上進行客製化，以加速開發並確保視覺一致性。

